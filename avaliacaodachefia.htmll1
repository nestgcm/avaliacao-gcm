<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GCM Salvador - Avaliação Encerrada</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    
    <style>
        :root {
            /* Cores Institucionais - Extraídas dos arquivos do sistema */
            --primary: #1a56db;
            --primary-dark: #0e2e71;
            --success: #10b981;
            --success-light: #f0fdf4;
            --light: #f3f4f6;
            --dark: #1f2937;
            --gray: #555e6b;
            --border-color: #e5e7eb;
            --border-radius: 8px;
            --shadow: 0 4px 6px -1px rgba(0,0,0,0.1), 0 2px 4px -2px rgba(0,0,0,0.1);
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        @keyframes pop {
            0% { transform: scale(0.7); opacity: 0; }
            80% { transform: scale(1.1); }
            100% { transform: scale(1); opacity: 1; }
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body, html {
            height: 100%;
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary) 100%);
            color: var(--dark);
            padding: 20px;
        }

        .container {
            width: 100%;
            max-width: 600px;
            background-color: #ffffff;
            border-radius: var(--border-radius);
            padding: 40px;
            text-align: center;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
            border-top: 8px solid var(--success); /* Destaque motivacional (verde) */
            animation: fadeIn 0.6s ease-out forwards;
        }

        .icon-container {
            width: 90px;
            height: 90px;
            margin: 0 auto 25px auto;
            background-color: var(--success);
            color: white;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 3.5rem;
            animation: pop 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
            animation-delay: 0.2s;
            opacity: 0; /* Começa invisível para a animação */
        }

        h1 {
            font-size: 2.2rem;
            font-weight: 700;
            color: var(--dark);
            margin-bottom: 10px;
        }

        p.lead {
            font-size: 1.15rem;
            color: var(--gray);
            line-height: 1.7;
            margin-bottom: 25px;
        }
        
        p.lead strong {
            color: var(--dark);
            font-weight: 600;
        }
        
        /* Card de Próximas Etapas */
        .next-steps-card {
            background-color: var(--success-light);
            border: 1px solid var(--success);
            border-radius: var(--border-radius);
            padding: 20px;
            margin-top: 25px;
            text-align: left;
        }
        
        .next-steps-card h3 {
            color: var(--success);
            font-weight: 700;
            text-transform: uppercase;
            font-size: 1rem;
            letter-spacing: 0.5px;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
        }
        
        .next-steps-card h3 i {
            margin-right: 8px;
            font-size: 1.1rem;
        }
        
        .next-steps-card p {
            font-size: 0.95rem;
            color: var(--dark);
            line-height: 1.6;
            margin-bottom: 10px;
        }
        
        .next-steps-card p:last-child {
            margin-bottom: 0;
        }
        
        .footer-logo {
            font-size: 1.8rem;
            color: var(--primary);
            margin-top: 30px;
        }

        @media (max-width: 480px) {
            .container {
                padding: 30px 25px;
            }
            h1 {
                font-size: 1.8rem;
            }
            p.lead {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>

    <div class="container">
        
        <div class="icon-container">
            <i class="fas fa-check-circle"></i>
        </div>

        <h1>Etapa Concluída!</h1>
        
        <p class="lead">
            O período para a <strong>Autoavaliação</strong> e <strong>Avaliação de Chefia</strong>, 
            conforme a Portaria 158/2025, foi finalizado em <strong>21 de outubro de 2025</strong>.
        </p>

        <div class="next-steps-card">
            <h3><i class="fas fa-tasks"></i>O que vem agora?</h3>
            <p>
                <strong>APURAÇÃO DOS DADOS:</strong> 
                Entre <strong>22/10 e 31/10/25</strong>, a Comissão de Avaliação fará a tabulação 
                e homologação de todos os resultados.
            </p>
            <p>
                <strong>PUBLICAÇÃO OFICIAL:</strong>
                Acompanhe a publicação dos resultados homologados no Diário Oficial do Município,
                prevista para o período de <strong>01/11 a 06/11/25</strong>.
            </p>
        </div>
        
        <div class="footer-logo">
            <i class="fas fa-shield-alt"></i>
            <p style="font-size: 0.9rem; color: var(--gray); margin-top: 10px;">Guarda Civil Municipal de Salvador</p>
        </div>
    </div>

</body>
</html>
